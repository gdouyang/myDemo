<?xml version="1.0" encoding="UTF-8"?>
<entity-mappings version="1.0"
        xmlns="http://java.sun.com/xml/ns/persistence/orm"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://java.sun.com/xml/ns/persistence/orm_1_0.xsd">
    <description>The minimal mappings for a persistent entity in XML.</description>
    <!-- 使用JQL -->
	<named-query name="findAllCustomersWithName">
	  <query>SELECT c FROM Customer c WHERE c.firstName LIKE :custName</query>
	</named-query>
	<!-- 使用原生SQL -->
	<named-native-query name="findAllCustomersWithNameNative">
		<query>SELECT c.* FROM Customer c WHERE c.firstName = :custName</query>
	</named-native-query>
	
</entity-mappings>